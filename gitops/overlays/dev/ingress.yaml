apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ecommerce-ingress
  annotations:
    # Remove rewrite-target to preserve the /api prefix
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
  - host: ecommerce-dev.local
    http:
      paths:
      - path: /api
        pathType: Prefix
        backend:
          service:
            # Use backend-stable for Argo Rollouts traffic management
            # Argo Rollouts will automatically manage traffic splitting between
            # backend-stable and backend-canary services
            name: backend-stable
            port:
              number: 3000
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend
            port:
              number: 80

# Made with Bob
