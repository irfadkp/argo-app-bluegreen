apiVersion: v1
kind: Service
metadata:
  name: backend-lamborghini
  labels:
    app: backend-lamborghini
    component: api
    shop: lamborghini
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: backend-lamborghini

# Made with Bob
