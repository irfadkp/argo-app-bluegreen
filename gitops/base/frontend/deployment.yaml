apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-lamborghini
  labels:
    app: frontend-lamborghini
    component: ui
    shop: lamborghini
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend-lamborghini
  template:
    metadata:
      labels:
        app: frontend-lamborghini
        component: ui
        shop: lamborghini
    spec:
      containers:
      - name: frontend
        image: ecommerce-frontend:1.0.13
        ports:
        - containerPort: 80
          name: http
        env:
        - name: API_URL
          valueFrom:
            configMapKeyRef:
              name: frontend-config
              key: api.url
        - name: INSTANA_EUM_KEY
          valueFrom:
            configMapKeyRef:
              name: frontend-config
              key: instana.eum.key
        - name: INSTANA_EUM_URL
          valueFrom:
            configMapKeyRef:
              name: frontend-config
              key: instana.eum.url
        resources:
          requests:
            memory: "128Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3

# Made with Bob
